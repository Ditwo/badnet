function initConvoc(sessionId, page, data)
{
  saveData('formConvoc');
  desactivate('formConvoc');
  return false;
}

function endSubmit(result)
{
  saveData('formConvoc');
  desactivate('formConvoc');
  return false;
}

  
